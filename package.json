{
  "name": "salesamore-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install --include=dev && npm run build && cd ../server && npm install",
    "start": "cd server && npm start"
  }
}
